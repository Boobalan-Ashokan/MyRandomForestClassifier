from setuptools import setup, find_packages

setup(
    name='MyRandomForestClassifier',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'numpy',
        # Add other dependencies here if needed
    ],
    author='Gautham,Boobalan'
)
